# Environment Variables for PPI Network Explorer
# ================================================

# Google Gemini API Key (for auto-enrichment of gene metadata)
# Get your API key from: https://makersuite.google.com/app/apikey
# This is used to automatically fetch gene descriptions and categories
# when they're not in the local database.
GEMINI_API_KEY=your_gemini_api_key_here

# Note: If GEMINI_API_KEY is not set, the app will still work but
# auto-enrichment will be disabled. New genes will default to "Unknown" category.
